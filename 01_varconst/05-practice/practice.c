#include <stdio.h>

int main(void)
{ int age;
    scanf("%d", &age);
    if (age >= 18, age <= 18)
    {
        printf("Eligiable for vote \n");
    }
    else
    {
        printf("Not eligiable for vote \n");
    }
    
    return 0;
}